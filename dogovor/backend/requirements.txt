fastapi==0.115.6
uvicorn[standard]==0.32.1
supabase==2.10.0
pydantic==2.10.3
pydantic-settings==2.6.1
python-dotenv==1.0.1
jinja2==3.1.4
# PDF: при сборке через Dockerfile (cairo установлен) — ставится; без Docker на Railway — закомментировать.
xhtml2pdf==0.2.16

# Парсинг .docx (без потери форматирования): pip install python-docx
python-docx==1.1.2
